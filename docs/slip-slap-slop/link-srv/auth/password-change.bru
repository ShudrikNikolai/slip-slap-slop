meta {
  name: password/change
  type: http
  seq: 5
}

post {
  url: {{URL}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "login": "{{LOGIN}}",
    "password":"{{PAS}}"
  }
}

vars:post-response {
  ACCESS_TOKEN: 
}

settings {
  encodeUrl: true
  timeout: 0
}
