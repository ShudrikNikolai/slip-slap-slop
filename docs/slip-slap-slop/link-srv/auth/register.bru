meta {
  name: register
  type: http
  seq: 2
}

post {
  url: {{URL}}/auth/register
  body: json
  auth: inherit
}

body:json {
  {
    "login": "{{LOGIN}}",
    "password":"{{PAS}}",
    "confirmPassword":"{{PAS}}"
  }
}

vars:post-response {
  ACCESS_TOKEN: 
}

settings {
  encodeUrl: true
  timeout: 0
}
